# Практическое задание №2. Пользовательские типы. Тип как экземпляр класса. Парсеры.


## Задание
### Часть 1.
1. Создайте и настройте проект MyEvolProject в Cabal
Добавьте дополнительную зависимость в проект: random
Создайте новый модуль MyEvolModule.hs в папке app.

2.Задайте тип-сумму MyEvolution в MyEvolModule.hs со следующими конструкторами и соответствующими строками (для класса типов Show):

Конструктор данных  | Соответствующая строка для классов типов Show
--------------------+---------------------------------------------- 
LUCA                | "Last Universal Common Ancestor"
Cyanobacteria       | "Synechococcus"
Trilobite           | "Paradoxides"
Ichthyostega        | "Ichthyostega"
Dimetrodon          | "Dimetrodon"
Archaeopteryx       | "Archaeopteryx"
Morganucodon        | "Morganucodon"
Purgatorius         | "Purgatorius"
Australopithecine   | "Australopithecus Afarensis"
Humans              | "Homo Sapiens"

Напишите вручную представителей классов типов: Show, Read, Eq, Ord, Enum, Bounded
Напишите аналогичный тип-сумму MyEvolution' и используя механизм deriving сделайте его представителем классов типов: Show, Read, Eq, Ord, Enum, Bounded

Добавьте в функцию main файла Main.hs вывод отсортированного списка значений типа MyEvolution. Используйте следующий код: putStrLn $ show $ sort ([...] :: [MyEvolution])

### Часть 2.


1. Создайте и настройте stack проект myParser

2. Создайте в src директорию MyTypes и создайте в нем три модуля со следующими реализациями:
- тип MyTree, который содержит значения в узлах и листьях. Сделайте этот тип представителем классов типов Foldable, Functor и Applicative (*)
- тип MyMaybe, аналогичный стандартному Maybe. Сделайте этот тип представителем классов типов Foldable, Semigroup, Monoid, Functor и Applicative (*)
- тип MyEither, аналогичный стандартному Either. Сделайте этот тип представителем классов типов Foldable, Semigroup, Functor и Applicative (*)
(*) для других необходимых классов типов можно использовать механизм deriving

Добавьте в конец каждого файла многострочный комментарий: 
для реализованного типа и определенных представителей классов типов напишите пример вызова соответствующих функций и результатов работы из ghci 
(если класс типов не требуется определять по заданию, то пример вызова не нужен):
- fold, foldMap, foldr
- (<>), sconcat, stimes
- mappend, mconcat
- fmap, (<$)
- pure, (<*>), liftA2, (*>), (<*)

3. Импортируйте модули в Main.hs



### Часть 3.

Расширьте проект myParser созданный в практическом задании 2 части 2 
- создайте в src директорию MyParsers для файлов этого задания
- скопируйте реализацию парсера из лекции в новый модуль. Модифицируйте разработанный на лекции парсер заменив стандартный тип Maybe на собственный тип MyMaybe из 2-ой части 2-го задания
- создайте два новых модуля и в каждом из них реализуйте функционал повторяющий лекционный материал, но с использованием библиотек Parsec и Attoparsec
- изолируйте решения друг от друга и вызовите их через точку входа в проекте используя следующий код (с точностью до квалифицированного импорта):

main :: IO ()
main = do
    putStrLn "MyParser:"
    putStrLn $ show (runParser plusOrMult "12*345dsf")
    putStrLn $ show (runParser plusOrMult "12+345dsf")
    putStrLn "Parsec:"
    putStrLn $ show (runParser plusOrMultParsec "12*345dsf")
    putStrLn $ show (runParser plusOrMultParsec "12+345dsf")
    putStrLn "Attoparsec:"
    putStrLn $ show (runParser plusOrMultAttoparsec "12*345dsf")
    putStrLn $ show (runParser plusOrMultAttoparsec "12+345dsf")

-}


## Основные возможности

1.Создан модуль MyEvolModule, в котором создан пользовательский тип данных MyEvolution. Далее MyEvolution делается экземпляром классов Show, Read, Ord, Eq, Enum и Bounded, без использования ключевого слова deriving.

2.Созданы модули MyTree, MyMaybe и MyEither, являющиеся пользовательскими реализациями бинарного дерева, монады Maybe и Either. Созданные типы делаются экземплярами классов Functor, Foldable, Applicative, Semigroup, Monoid.

3. Создан модуль ParserEli, повторяющий лекционный материал. В нем реализован парсер простых ошибок. В отличие от лекционного материала, в данном модуле использование Maybe заменяется использованием MyMaybe. Так же созданы модули ParsecEli и AttoparsecEli, повторяющие функционал из лекционного материала, но использующие библиотеки Parsec и Attoparsec. Созданные модули импортированы в модуль Main.

## Использование

Перед созданием каждого модуля была использована система сборки stack. Для этого в командной строке необходимо было прописать команду

```bash
stack build
```

Далее, когда все необходимые библиотеки были загружены, можно запустить проект при помощи команды
```bash
stack run
```

Созданы все необходимые файлы для сборки. Модуль Main находится в директории app, вспомогательные модули находятся в директории src.



# Документация коммитов к practice_02

## Коммит "Отработка замечания: приведение репозитория в порядок", 25.03.2026

Были удалены почти все лишние файлы из директории с практическими заданиями. 


## Коммит "Использование Cabal в работе; создание типа MyEvolution", 26.03.2026

Был создан проект при помощи команд 

```cabal init```

```cabal build```

Последняя команда загрузила и скомпилировала все модули и проект.

Команда 

```cabal run``` 

запускает программу.

Далее в файле был создан тип MyEvolution, после чего был сделан вручную экземпляром класса Show и и частично класса Read.


## Коммит "Ручное написание представителей классов типов Show, Read, Eq, Ord", 26.03.2026

Созданный тип MyEvolution был сделан экземпляром классов Read, Eq, Ord, Enum.


## Коммит "Добавление сортировки в функцию main файла Main.hs", 26.03.2026

В модуль "Main.hs" были импортированы модуль MyEvolModule, описанный ранее, и Data.List. Далее в Main.hs был добавлен код для отображения отсортированного списка из файла practice_02.hs. Была исправлена реализация становления типа MyEvolution экземпляром класса Read и Enum.


## Коммит "Выполнил pr02_1.hs, реализовал типы для pr02_2.hs и написал часть комментариев для MyTree.hs", 02.04.2026

Создана директория с проектом myParser после введения следующем команды:

```stack new myParser```

Далее в папке src создана директория MyTypes Она содержит три файла: MyMaybe.hs, MyTree.hs и MyEither.hs. В каждом из этих файлов созданы кастомные типы MyMaybe, MyTree и MyEither, подобные типам Maybe, Tree (в нашем случае -- бинарное дерево) и Either. Далее каждый из типов делается экземпляром классов Foldable, Functor, Applicative, Semigroup, Monoid (каждый из созданных типов становится экземпляром только указанных для него классов).



## Коммит "Начал писать большой комментарий к MyMaybe", 02.04.2026

Написаны комментарии, в которых продемонстрирована работа функции foldr для элементов типа MyMaybe, и функций <*, *>, для класса MyTree.


## Коммит "добавил комментарии к foldr", 02.04.2026

Добавлен комментарий в файле MyMaybe.hs, показывающий корректно работающую функцию foldr для элементов типа MyMaybe.


## Коммит "Рефакторинг типа MyTree: было розовое дерево, стало бинарное дерево; Рефакторинг соответствующих комментариев по типу MyTree", 02.04.2026

Переопределен тип MyTree: было розовое дерево, стало бинарное дерево. Исправлены соответствующие функции, делающие тип MyTree экземпляром классов Functor, Foldable и Applicative. Исправлены комментарии, демонстрирующие работу этих функций с элементами типа MyTree.


## Коммит "Рефакторинг функции pure и (<*>), теперь выполняется закон fmap f tree <=> (pure f) <*> tree", 02.04.2026

Исправление функции <*> и pure для становления типа MyTree экземпляром класса Applicative. Добавлены соответствующие комментарии, демонстрирующие работу функций.


## Коммит "Рефакторинг комментариев к типу MyTree: теперь все комментарии написаны корректно", 02.04.2026

Добавлены комментарии по работе функций <*>, <*, *>, liftA2 для типа MyTree, не отраженных в комментариях ранее. 


## Коммит "Добавление комментариев для типа MyEither", 02.06.2026

Добавление комментариев работы функций для типа MyEither, добавление классов с помощью механизма deriving. Изменение функции <> для типа MyMaybe, а также добавление комментариев для типа MyMaybe.


## Коммит "Удаление директории с лекциями", 02.06.2026

Перемещение директории с лекциями lecs за репозиторий с практическими заданиями.


## Коммит "сборка проекта для practice_03, добавление некоторых функций в ParsecEli.hs", 03.04.2026

Использование функционала, данного в лекции, лишь с заменой Maybe на MyMaybe, в файле ParserEli.hs. Создание файлов ParsecEli.hs и AttoparsecEli.hs, представляющих собой парсер, но реализованные с помощью Parsec и Attoparsec. Добавление в файл ParsecEli.hs реализаций функций charA и mySatisfy.


## Коммит "Добавление функций myDigit myLower myChar", 03.04.2026

Добавление в файл ParsecEli.hs функций myChar, myLower, myDigit и runParser, проверяющих, является ли первый символ указанным в параметре myChar, с низким регистром или цифрой. Функция runParser -- функция, запускающая парсер.


## Коммит "Добавление runParser в ParsecEli.hs, исполнение файла Main.o с MyParser и Parsec", 03.04.2026

Переопределение функций runParser (функция, запускающая парсер) и plusOrMult (вычисляющей либо сложение, либо произведение чисел в стркое), runParserParsec и plusOrMultParsec в модуле Main и добавление туда кода, данного в practice_03.hs. Добавление в AttoParsecEli.hs реализации функции charAAttoparsec, определяющией, является ли первый символ буквой 'A'. Добавление в файле ParsecEli.hs ко всем названиям функций окончания "-Parsec" (charA -> charAParsec, и тд). Добавление функций digitsParsec (возвращает число в строке), finalMultParsec (возвращает результат произведения двух чисел в строке), finalPlusParsec (возвращает результат произведения двух чисел в строке) и plusOrMultParsec (объединение двух функций выше) в файл ParsecEli.hs.


## Коммит "Добавление функций для AttoparsecEli.hs и добавление соответствующих строк кода в Main.hs", 03.04.2026

Переопределение функций runParser и plusOrMultAttoparsec из файла AttoparsecEli.hs в модуле Main. Добавление соответствующих строк кода в модуль Main из practice_03.hs. Добавление функций satisfyAttoparsec (проверка, удовлетворяет ли функция предикату, передаваемому в параметре), charAttoparsec (проверка, является ли первый символ строки символом, переданным в параметре), lowerAttoparsec (проверка, является ли первый символ в нижним регистре), digitsAttoparsec (возвращает число в строке), finalMultAttoparsec (произведение чисел в строке), plusOrMultAttoparsec (сложение или произведение чисел в строке) и runParser (функция, запускающая парсер) в файле AttoparsecEli.hs.


## Коммит "Добавление readme файла", 03.04.2026

Добавление файла README.rm, содержащая документацию к выполнению практической работы practice_02.


## Коммит "Добавление описания к README.md файлу", 03.04.2026

Добавление условия задания и начала описания основных возможностей.


## Коммит "Добавление описания к практическому заданию 2", 03.04.2026

Добавление основных возможностей и рекомендаций к использованию



## Коммит "Исправление недочетов в функциях runParser", 03.04.2026

Исправление функции runParser в файле ParsecEli.hs: теперь при парсинге выводится только результат, а не исходная строка и полученный результат.


## Коммит "Убрал лишние файлы *.o *.hi", 03.04.2026

Удаление лишних файлов из репозитория, созданные при сборке проекта в неверном месте. Теперь лишних файлов (*.o, *.hi) нет.


## Коммит "practice_02-ver.2026-04-09-01-55", 09.04.2026

Удаление файла с лекцией из репозитория, импорт комбинатора парсера try в модуль ParsecEli.hs для отката парсинга в случае неудачи при использовании функции plusOrMultParsec. 